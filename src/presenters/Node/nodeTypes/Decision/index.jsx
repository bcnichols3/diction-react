import React from "react";
import "./style.scss";

/* -----------------    COMPONENT     ------------------ */

const Decision = (props) => (
	<div className={props.className} />
);

export default Decision;
